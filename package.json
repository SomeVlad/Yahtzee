{
  "name": "yaz",
  "module": "server.ts",
  "type": "module",
  "scripts": {
    "start": "",
    "build:dev": "bun run scripts/build.ts && bun build src/index.ts --outdir ./build --watch",
    "serve:dev": "bun --hot server.ts",
    "build": "bun run scripts/build.ts",
    "serve": "bun server.ts"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "prettier": "^3.2.5",
    "typescript": "^5.3.3"
  },
  "dependencies": {
    "phaser": "^3.70.0",
    "rambda": "^9.1.0"
  }
}
